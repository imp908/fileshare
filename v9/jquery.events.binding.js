//=========================On Window Resize=================================
$(window).resize(function () {
//  resizeGal();
})
//=========================On Window Resize=================================

//=========================On Body Mousemove=================================
$('body').mousemove(function (e) { })
//=========================On Body Mousemove=================================

//=========================On Document Ready=================================
$(document).ready(function () {
  // galInit();
  // console.log(data.context)
  // $('#dialog10').scope().cnt = data.context
  // $('#dialog10').scope().$apply()
  // metroDialog.toggle('#dialog10')
  // console.log( $('body').scope().Vacations)
})
//=========================On Document Ready=================================