@model Presentation_.Models.MainModel

@{
    ViewBag.Title = "Presentations";
}

<p>Navbars:</p>
<div class="navbar navbar-header">
    <div class="container">
        <div class="navbar-header">
            <button type="button" class="navbar navbar-toggle collapsed" data-toggle="collapse" data-target=".navbar-collapse">
                <span class="icon-bar"></span>
                <span class="icon-bar"></span>
            </button>
        </div>
        <a class="navbar-brand" href="#">Menu</a>
    </div>
    <div class="navbar-collapse collapse">
        <ul class="nav navbar-nav"
            <li class="active"><a href="#">One</a></li>
            <li><a href="#">One</a></li>
        <li class="dropdown">
            <a href="#" class="dropdown-toggle" data-toggle="dropdown"></a>
        </li>
        </ul>
    </div>
</div>


<h2>Presentations</h2>
<div class="container">
    <table class="table table-striped">
        <tr>
            <td>@Html.DisplayNameFor(m => m.FD_ACQ_D_ent.DT_REG)</td>
            <td>@Html.DisplayNameFor(m => m.FD_ACQ_D_ent.PAY_SYS)</td>
            <td>@Html.DisplayNameFor(m => m.FD_ACQ_D_ent.TYPE_TRANSACTION)</td>
            <td>@Html.DisplayNameFor(m => m.FD_ACQ_D_ent.AMT)</td>
        </tr>
        @foreach (var a in Model.FD_ACQ)
        {
            <tr>
                <td>@Html.DisplayFor(m => a.DT_REG)</td>
                <td>@Html.DisplayFor(m => a.PAY_SYS)</td>
                <td>@Html.DisplayFor(m => a.TYPE_TRANSACTION)</td>
                <td>@Html.DisplayFor(m => a.AMT)</td>
            </tr>
        }

    </table>
</div>

    <svg id="barChart"></svg>

    <table id="TableRes"></table>
